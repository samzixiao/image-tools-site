import { useEffect, useRef, useState } from 'react'
import type { ChangeEvent, PointerEvent } from 'react'
import './App.css'

type Preset = { id:string; group:string; name:string; en:string; width:number; height:number }
type Format = 'image/jpeg'|'image/png'|'image/webp'
type Shape = 'original'|'circle'|'heart'|'star'|'sticker'
const presets:Preset[] = [
  {id:'ig-square',group:'Instagram',name:'帖子（正方形）',en:'Post square',width:1080,height:1080},
  {id:'ig-story',group:'Instagram',name:'快拍 / Reels',en:'Story / Reel',width:1080,height:1920},
  {id:'tiktok',group:'TikTok',name:'视频封面',en:'Video cover',width:1080,height:1920},
  {id:'youtube',group:'YouTube',name:'视频缩略图',en:'Thumbnail',width:1280,height:720},
  {id:'amazon',group:'Amazon',name:'商品主图',en:'Product image',width:2000,height:2000},
  {id:'etsy',group:'Etsy',name:'商品图片',en:'Listing image',width:2000,height:1600},
  {id:'shopify',group:'Shopify',name:'商品图',en:'Product image',width:2048,height:2048},
  {id:'og',group:'Website',name:'分享卡片',en:'Open Graph card',width:1200,height:630},
]

function App(){
  const [url,setUrl]=useState(''),[name,setName]=useState(''),[size,setSize]=useState({width:0,height:0})
  const [preset,setPreset]=useState(presets[0]),[custom,setCustom]=useState({width:1080,height:1080})
  const [mode,setMode]=useState<'crop'|'fit'>('crop'),[format,setFormat]=useState<Format>('image/jpeg'),[quality,setQuality]=useState(90)
  const [shape,setShape]=useState<Shape>('original'),[measureOn,setMeasureOn]=useState(false),[unit,setUnit]=useState<'cm'|'in'>('cm')
  const [dimensions,setDimensions]=useState({width:'60',height:'40',depth:'20'}),[label,setLabel]=useState('')
  const [crop,setCrop]=useState({x:0,y:0,width:1,height:1}),[drag,setDrag]=useState<{x:number;y:number;crop:typeof crop}|null>(null)
  const input=useRef<HTMLInputElement>(null), output=preset.id==='custom'?custom:{width:preset.width,height:preset.height}, aspect=output.width/output.height

  useEffect(()=>{if(!url)return;const img=new Image();img.onload=()=>{setSize({width:img.naturalWidth,height:img.naturalHeight});const a=img.naturalWidth/img.naturalHeight;if(a>aspect){const w=aspect/a;setCrop({x:(1-w)/2,y:0,width:w,height:1})}else{const h=a/aspect;setCrop({x:0,y:(1-h)/2,width:1,height:h})}};img.src=url},[url,aspect])
  function load(file?:File){if(!file||!file.type.startsWith('image/'))return;setName(file.name);setUrl(URL.createObjectURL(file))}
  function choose(p:Preset){setPreset(p);if(p.id!=='custom')setCustom({width:p.width,height:p.height})}
  function down(e:PointerEvent<HTMLDivElement>){if(!url||mode==='fit')return;e.currentTarget.setPointerCapture(e.pointerId);setDrag({x:e.clientX,y:e.clientY,crop})}
  function move(e:PointerEvent<HTMLDivElement>){if(!drag)return;const b=e.currentTarget.getBoundingClientRect(),dx=(e.clientX-drag.x)/b.width,dy=(e.clientY-drag.y)/b.height;setCrop(c=>({...c,x:Math.min(Math.max(0,drag.crop.x+dx),1-c.width),y:Math.min(Math.max(0,drag.crop.y+dy),1-c.height)}))}
  function zoom(delta:number){if(!url)return;const iw=size.width/size.height,w=Math.min(1,Math.max(aspect/iw,crop.width+delta)),h=Math.min(1,w/aspect*iw);setCrop({width:w,height:h,x:Math.min(crop.x,1-w),y:Math.min(crop.y,1-h)})}
  function drawShape(ctx:CanvasRenderingContext2D,w:number,h:number){ctx.beginPath();if(shape==='circle'){ctx.ellipse(w/2,h/2,w/2,h/2,0,0,Math.PI*2);return}if(shape==='heart'){ctx.moveTo(w/2,h*.9);ctx.bezierCurveTo(-w*.08,h*.55,w*.08,h*.12,w*.3,h*.18);ctx.bezierCurveTo(w*.43,h*.02,w*.5,h*.18,w/2,h*.28);ctx.bezierCurveTo(w*.5,h*.18,w*.57,h*.02,w*.7,h*.18);ctx.bezierCurveTo(w*.92,h*.12,w*1.08,h*.55,w/2,h*.9);return}if(shape==='star'){for(let i=0;i<10;i++){const r=i%2?w*.23:w*.48,a=-Math.PI/2+i*Math.PI/5,x=w/2+Math.cos(a)*r,y=h/2+Math.sin(a)*r;if(i===0)ctx.moveTo(x,y);else ctx.lineTo(x,y)}ctx.closePath();return}if(shape==='sticker'){const r=Math.min(w,h)*.09;ctx.roundRect(0,0,w,h,r);return}ctx.rect(0,0,w,h)}
  function drawAnnotations(ctx:CanvasRenderingContext2D,w:number,h:number){if(!measureOn)return;ctx.save();ctx.strokeStyle='#e66d5b';ctx.fillStyle='#e66d5b';ctx.lineWidth=Math.max(3,w/500);ctx.font=`bold ${Math.max(18,w/42)}px Arial`;const pad=Math.max(30,w/18), arrow=(x:number,y:number,dx:number,dy:number)=>{ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+dx,y+dy);ctx.stroke();ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+dx*.12-dy*.12,y+dy*.12+dx*.12);ctx.lineTo(x+dx*.12+dy*.12,y+dy*.12-dx*.12);ctx.closePath();ctx.fill()};const unitText=unit==='cm'?`${dimensions.width} cm / ${(Number(dimensions.width)/2.54).toFixed(1)} in`:`${dimensions.width} in / ${(Number(dimensions.width)*2.54).toFixed(1)} cm`;ctx.beginPath();ctx.moveTo(pad,h-pad);ctx.lineTo(w-pad,h-pad);ctx.stroke();arrow(pad,h-pad,pad?18:0,0);arrow(w-pad,h-pad,-18,0);ctx.fillText(unitText,pad,h-pad-12);const heightText=unit==='cm'?`${dimensions.height} cm / ${(Number(dimensions.height)/2.54).toFixed(1)} in`:`${dimensions.height} in / ${(Number(dimensions.height)*2.54).toFixed(1)} cm`;ctx.save();ctx.translate(pad-12,h-pad);ctx.rotate(-Math.PI/2);ctx.fillText(heightText,0,0);ctx.restore();if(label)ctx.fillText(label,pad,pad+20);ctx.restore()}
  function download(){if(!url)return;const img=new Image();img.onload=()=>{const c=document.createElement('canvas');c.width=output.width;c.height=output.height;const ctx=c.getContext('2d')!;ctx.fillStyle='#fff';ctx.fillRect(0,0,c.width,c.height);const sx=mode==='fit'?0:size.width*crop.x,sy=mode==='fit'?0:size.height*crop.y,sw=mode==='fit'?size.width:size.width*crop.width,sh=mode==='fit'?size.height:size.height*crop.height,scale=Math.min(c.width/sw,c.height/sh),dw=sw*scale,dh=sh*scale,dx=(c.width-dw)/2,dy=(c.height-dh)/2;ctx.save();if(shape!=='original')drawShape(ctx, c.width,c.height);else ctx.rect(dx,dy,dw,dh);ctx.clip();ctx.drawImage(img,sx,sy,sw,sh,dx,dy,dw,dh);ctx.restore();drawAnnotations(ctx,c.width,c.height);const ext=format.split('/')[1].replace('jpeg','jpg'),a=document.createElement('a');a.download=`${name.replace(/\.[^.]+$/,'')||'image'}-${output.width}x${output.height}.${ext}`;a.href=c.toDataURL(format,quality/100);a.click()};img.src=url}
  const cropText=`${Math.round(crop.width*size.width)} × ${Math.round(crop.height*size.height)} px`
  return <div className="app-shell"><header className="topbar"><a className="brand" href="#top"><span>✦</span> SizeCraft</a><nav><a href="#tool">Image Resizer</a><a href="#ecommerce">E-commerce tools</a><a href="#how">How it works</a></nav><i>● 100% browser-based</i></header><main id="top"><section className="hero"><p className="eyebrow">FAST · PRIVATE · FREE</p><h1>Make every image the <em>right size.</em></h1><p>Resize, shape-crop, and annotate product images for social media, shops, websites, and everyday tasks.</p></section><section className="workspace" id="tool"><aside className="controls"><Step n="01" title="Upload your image" sub="JPG, PNG, WebP · up to 50 MB"/><button className="upload" onClick={()=>input.current?.click()}><b>↑</b>Drop an image here<br/><small>or click to browse</small></button><input ref={input} type="file" accept="image/jpeg,image/png,image/webp,image/gif" hidden onChange={(e:ChangeEvent<HTMLInputElement>)=>load(e.target.files?.[0])}/>{name&&<p className="file">✓ {name} <span>{size.width} × {size.height}px</span></p>}<Step n="02" title="Choose a size" sub="Pick a platform or enter your own"/><div className="presets">{presets.map(p=><PresetButton key={p.id} p={p} active={preset.id===p.id} onClick={()=>choose(p)}/>)}<button className={preset.id==='custom'?'preset active':'preset'} onClick={()=>choose({id:'custom',group:'Custom',name:'自定义尺寸',en:'Custom size',width:custom.width,height:custom.height})}><b>Custom</b><span>自定义尺寸</span><small>Enter your size</small></button></div>{preset.id==='custom'&&<div className="custom"><label>Width<input type="number" min="1" value={custom.width} onChange={e=>setCustom({...custom,width:Number(e.target.value)})}/></label><b>×</b><label>Height<input type="number" min="1" value={custom.height} onChange={e=>setCustom({...custom,height:Number(e.target.value)})}/></label></div>}<Step n="03" title="Choose a shape" sub="Create a profile, sticker, or product cutout"/><div className="shape-grid">{(['original','circle','heart','star','sticker'] as Shape[]).map(s=><button key={s} className={shape===s?'shape active':'shape'} onClick={()=>setShape(s)}><span className={`shape-icon ${s}`}>{s==='heart'?'♥':s==='star'?'★':s==='circle'?'●':s==='sticker'?'▣':'□'}</span><small>{s==='original'?'Original':s[0].toUpperCase()+s.slice(1)}</small></button>)}</div><Step n="04" title="Add product details" sub="Optional bilingual size markers"/><label className="check"><input type="checkbox" checked={measureOn} onChange={e=>setMeasureOn(e.target.checked)}/> Add dimension arrows</label>{measureOn&&<div className="annotation"><div className="unit-tabs"><button className={unit==='cm'?'active':''} onClick={()=>setUnit('cm')}>cm + inch</button><button className={unit==='in'?'active':''} onClick={()=>setUnit('in')}>inch + cm</button></div><div className="three-inputs"><label>W<input value={dimensions.width} onChange={e=>setDimensions({...dimensions,width:e.target.value})}/></label><label>H<input value={dimensions.height} onChange={e=>setDimensions({...dimensions,height:e.target.value})}/></label><label>D<input value={dimensions.depth} onChange={e=>setDimensions({...dimensions,depth:e.target.value})}/></label></div><input className="label-input" placeholder="Product name or feature label (optional)" value={label} onChange={e=>setLabel(e.target.value)}/><small className="hint">真实尺寸需要人工测量或已知参照物；照片本身不能自动推断准确厘米。</small></div>}<Step n="05" title="How should it fit?" sub="Choose how to handle the crop"/><div className="modes"><button className={mode==='crop'?'active':''} onClick={()=>setMode('crop')}>Crop to fill<small>Best for social posts</small></button><button className={mode==='fit'?'active':''} onClick={()=>setMode('fit')}>Fit with padding<small>Keep the full image</small></button></div></aside><section className="preview"><div className="preview-head"><div><small>LIVE PREVIEW</small><strong>{preset.group} · {preset.en}</strong></div><b>{output.width} × {output.height} px</b></div><div className="stage" style={{aspectRatio:`${output.width}/${output.height}`}} onPointerDown={down} onPointerMove={move} onPointerUp={()=>setDrag(null)}>{url?<><img src={url} alt="Preview" className={`preview-image ${shape}`} style={{objectFit:mode==='fit'?'contain':'cover'}}/><div className={mode==='fit'?'crop hidden':'crop'} style={{left:`${crop.x*100}%`,top:`${crop.y*100}%`,width:`${crop.width*100}%`,height:`${crop.height*100}%`}}><span>DRAG TO REPOSITION</span></div>{measureOn&&<div className="measure-preview"><span>{dimensions.width} cm / {(Number(dimensions.width)/2.54).toFixed(1)} in</span><i></i><span>{label||'Product dimensions'}</span></div>}</>:<div className="empty"><b>▧</b><strong>Your preview appears here</strong><small>Upload an image to get started</small></div>}</div><div className="preview-foot"><span>{url?`${cropText} crop area`:'No image selected'}</span><div><button onClick={()=>zoom(-.05)} disabled={!url}>−</button> Zoom <button onClick={()=>zoom(.05)} disabled={!url}>＋</button></div></div><div className="export"><label>Format <select value={format} onChange={e=>setFormat(e.target.value as Format)}><option value="image/jpeg">JPG</option><option value="image/png">PNG</option><option value="image/webp">WebP</option></select></label>{format!=='image/png'&&<label>Quality <input type="range" min="50" max="100" value={quality} onChange={e=>setQuality(Number(e.target.value))}/> {quality}%</label>}<button className="download" onClick={download} disabled={!url}>Download image <b>↓</b></button></div></section></section><section className="tool-cards" id="ecommerce"><p className="eyebrow">BUILT FOR EVERYDAY WORK</p><h2>One image tool, many practical jobs.</h2><div><article><b>↔</b><h3>Product dimension maker</h3><p>Add width, height, depth, cm and inch labels to furniture, clothing, crafts, and marketplace listings.</p></article><article><b>✦</b><h3>Shape crop & stickers</h3><p>Make profile pictures, product badges, heart cutouts, star stickers, and rounded thumbnails without an app.</p></article><article><b>▦</b><h3>Marketplace presets</h3><p>Prepare product images for Amazon, Etsy, Shopify, social posts, and website sharing cards.</p></article></div></section><section className="how" id="how"><p className="eyebrow">HOW IT WORKS</p><h2>From idea to image in three steps.</h2><div><Feature icon="01" title="Upload" text="Drop in an image from your device. It never leaves your browser."/><Feature icon="02" title="Design" text="Choose a platform, shape, size label, or custom detail."/><Feature icon="03" title="Download" text="Export a ready-to-use JPG, PNG, or WebP image."/></div></section></main><footer><span>© 2026 SizeCraft</span><span>Free image tools for everyday work.</span></footer></div>
}
function Step({n,title,sub}:{n:string;title:string;sub:string}){return <div className="step"><span>{n}</span><div><strong>{title}</strong><small>{sub}</small></div></div>}
function PresetButton({p,active,onClick}:{p:Preset;active:boolean;onClick:()=>void}){return <button className={active?'preset active':'preset'} onClick={onClick}><b>{p.group}</b><span>{p.name}</span><small>{p.width} × {p.height}</small></button>}
function Feature({icon,title,text}:{icon:string;title:string;text:string}){return <div className="feature"><b>{icon}</b><span><strong>{title}</strong><small>{text}</small></span></div>}
export default App
